{should, sinon, auth} = require("./base")()

search = require "../scripts/coffee/search"

describe "Search", ->

  it "should handle observable return"

  it "should handle errors gracefully"

  it "must handle disconnected state"